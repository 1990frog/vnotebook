[TOC]

设计模式的一个重要原则就是：别改代码，只需要添代码，以前所有的老代码，都是有价值的，需要尽力保留

# 单一职责原则（Single Responsibility Principle）

# 里氏替换原则

# 依赖导致原则

# 接口隔离原则

# 迪米特原则

# 开闭原则
+ 定义：一个软件实体如类、模块和函数应该扩展开发，对修改关闭
+ 用抽象构建框架，用实现扩展细节
+ 优点：提高软件系统的可复用性及可维护性

system.out里面有锁

接口是契约

double丢失精度

